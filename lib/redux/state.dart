import 'package:reduxgooglemapdemo/database.dart';

class LocationState{
  Future<List<LocationData>> locationData;
  LocationState({required this.locationData});
  // double latitude;
  // double longitude;
  //
  // LocationState({required this.longitude ,required this.latitude});
}